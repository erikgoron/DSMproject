clear all
global tspan body Nbody dist

Filename='4bardyn.rtf';

Readinputdata

% Vector with initial conditions y0
y0=q2y(body,Nbody);

[t,y]=ode45(@FuncEval,tspan,y0);

for i=1:Nbody
    Position(:,)
%Post process data and report
figure;
axis equal
hold on
plot(y(:,4),y(:,5));
plot(y(:,7),y(:,8));
plot(y(:,10),y(:,11));
hold off
figure 
hold on

plot(tspan,dist);
hold off